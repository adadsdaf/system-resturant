<Window x:Class="RestaurantMS.Desktop.Views.Inventory.StockMovementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        FlowDirection="RightToLeft" Background="{StaticResource BgBrush}"
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TxtTitle" Grid.Row="0"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="المادة" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,0,0,6"/>
            <ComboBox x:Name="CmbItem" Style="{StaticResource DarkCombo}"
                      Height="42" Margin="0,0,0,12"
                      DisplayMemberPath="Display" SelectedValuePath="ingredient_id"/>
            <TextBlock Text="الكمية" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,0,0,6"/>
            <TextBox x:Name="TxtQty" Style="{StaticResource DarkInput}" Height="42" Margin="0,0,0,12" Text="0"/>
            <TextBlock Text="ملاحظات" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,0,0,6"/>
            <TextBox x:Name="TxtNotes" Style="{StaticResource DarkInput}" Height="42"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
            <Button Content="تسجيل" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click" Margin="0,0,8,0"/>
            <Button Content="إلغاء" Style="{StaticResource GhostButton}" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
