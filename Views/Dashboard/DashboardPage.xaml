<Page x:Class="RestaurantMS.Desktop.Views.Dashboard.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FlowDirection="RightToLeft"
      Background="#0d1117">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <!-- رأس الصفحة -->
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock x:Name="TxtGreeting" Text="مرحباً 👋"
                               Foreground="White" FontSize="22" FontWeight="Bold"/>
                    <TextBlock x:Name="TxtDate" Foreground="#8b949e" FontSize="13"/>
                </StackPanel>
                <Button Content="🔄  تحديث البيانات" HorizontalAlignment="Left"
                        Style="{StaticResource GhostButton}"
                        VerticalAlignment="Center" Click="BtnRefresh_Click"/>
            </Grid>

            <!-- بطاقات الإحصائيات - صف 1 -->
            <UniformGrid Columns="3" Margin="0,0,0,16">

                <Border Margin="0,0,8,0" CornerRadius="12" Padding="20" BorderThickness="0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1B4E9E" Offset="0"/>
                            <GradientStop Color="#0d3070" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#ffffff22" CornerRadius="10" Width="44" Height="44">
                                <TextBlock Text="💰" FontSize="22" HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="مبيعات اليوم" Foreground="#ffffffaa" FontSize="12"/>
                                <TextBlock Text="اليوم" Foreground="#ffffff66" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock x:Name="TxtSalesToday" Text="0.00 ريال"
                                   Foreground="White" FontSize="24" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtOrdersToday" Text="0 طلب مكتمل"
                                   Foreground="#ffffff88" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Margin="4,0" CornerRadius="12" Padding="20">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#F7941D" Offset="0"/>
                            <GradientStop Color="#c4700a" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#ffffff22" CornerRadius="10" Width="44" Height="44">
                                <TextBlock Text="👨‍🍳" FontSize="22" HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="طلبات المطبخ" Foreground="#ffffffaa" FontSize="12"/>
                                <TextBlock Text="قيد التحضير" Foreground="#ffffff66" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock x:Name="TxtPendingKitchen" Text="0"
                                   Foreground="White" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="طلب لم يُنجز بعد"
                                   Foreground="#ffffff88" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Margin="8,0,0,0" CornerRadius="12" Padding="20">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#b91c1c" Offset="0"/>
                            <GradientStop Color="#7f1d1d" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#ffffff22" CornerRadius="10" Width="44" Height="44">
                                <TextBlock Text="⚠️" FontSize="22" HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="تنبيهات المخزون" Foreground="#ffffffaa" FontSize="12"/>
                                <TextBlock Text="تحت الحد الأدنى" Foreground="#ffffff66" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock x:Name="TxtLowStock" Text="0"
                                   Foreground="White" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="مادة تحتاج إعادة طلب"
                                   Foreground="#ffffff88" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- بطاقات - صف 2 -->
            <UniformGrid Columns="4" Margin="0,0,0,20">

                <Border Margin="0,0,6,0" Background="#161b22" CornerRadius="10"
                        Padding="16" BorderBrush="#21262d" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="📋" FontSize="24" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtActiveItems" Text="0"
                                   Foreground="#F7941D" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="صنف في القائمة"
                                   Foreground="#8b949e" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Margin="3,0" Background="#161b22" CornerRadius="10"
                        Padding="16" BorderBrush="#21262d" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="📅" FontSize="24" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtReservations" Text="0"
                                   Foreground="#1B4E9E" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="حجز اليوم"
                                   Foreground="#8b949e" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Margin="3,0" Background="#161b22" CornerRadius="10"
                        Padding="16" BorderBrush="#21262d" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="👥" FontSize="24" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtCustomers" Text="0"
                                   Foreground="#3fb950" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="عميل مسجّل"
                                   Foreground="#8b949e" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Margin="6,0,0,0" Background="#161b22" CornerRadius="10"
                        Padding="16" BorderBrush="#21262d" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="📦" FontSize="24" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtIngredients" Text="0"
                                   Foreground="#a371f7" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="مادة في المخزون"
                                   Foreground="#8b949e" FontSize="11"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- آخر الطلبات -->
            <Border Background="#161b22" CornerRadius="12"
                    Padding="20" BorderBrush="#21262d" BorderThickness="1">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="آخر الطلبات" Foreground="White"
                                       FontSize="15" FontWeight="Bold"/>
                            <Border Background="#1c2d4a" CornerRadius="20"
                                    Padding="10,3" Margin="12,0,0,0">
                                <TextBlock x:Name="TxtOrderCount" Text="0"
                                           Foreground="#F7941D" FontSize="12" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                        <Button Content="عرض الكل ←" Style="{StaticResource GhostButton}"
                                HorizontalAlignment="Left" FontSize="12"/>
                    </Grid>

                    <DataGrid x:Name="GridOrders" Style="{StaticResource DataGridStyle}"
                              AutoGenerateColumns="False" MaxHeight="280">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم"         Binding="{Binding order_id}"       Width="70"/>
                            <DataGridTextColumn Header="العميل"      Binding="{Binding customer_name}"  Width="*"/>
                            <DataGridTextColumn Header="الإجمالي"    Binding="{Binding total_fmt}"      Width="120"/>
                            <DataGridTextColumn Header="طريقة الدفع" Binding="{Binding payment_method}" Width="110"/>
                            <DataGridTextColumn Header="الكاشير"     Binding="{Binding served_by}"      Width="130"/>
                            <DataGridTextColumn Header="الوقت"       Binding="{Binding time_fmt}"       Width="130"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
